# AIRE — Python Dependencies
# Pin major versions; patch versions are unpinned for security updates.

# ── Google Cloud ──────────────────────────────────────────────────────────────
google-cloud-secret-manager>=2.20,<3.0
google-cloud-aiplatform>=1.60,<2.0
google-cloud-discoveryengine>=0.13,<1.0
google-cloud-storage>=2.16,<3.0
google-auth>=2.29,<3.0
google-auth-httplib2>=0.2,<1.0

# ── Vertex AI / Gemini ────────────────────────────────────────────────────────
vertexai>=1.60,<2.0
google-generativeai>=0.8,<1.0

# ── OpenTelemetry ─────────────────────────────────────────────────────────────
opentelemetry-api>=1.25,<2.0
opentelemetry-sdk>=1.25,<2.0
opentelemetry-exporter-otlp-proto-http>=1.25,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.25,<2.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-requests>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0

# ── Data & ML ─────────────────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.2,<3.0

# ── Config & Utilities ────────────────────────────────────────────────────────
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
tenacity>=8.3,<9.0          # Retry with backoff
structlog>=24.0,<25.0       # Structured logging

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-mock>=3.14,<4.0
pytest-cov>=5.0,<6.0
